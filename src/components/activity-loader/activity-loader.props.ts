import { ActivityIndicatorProps } from "react-native";

/* eslint-disable semi */
export default interface PrimaryActivityIndicatorProps extends ActivityIndicatorProps {
  // Loading text for user
  text?: string;
}
